{application, rexi, [
    {description, "Lightweight RPC server"},
    {vsn, git},
    {registered, [rexi_sup, rexi_server]},
    {applications, [kernel, stdlib, config, folsom]},
    {mod, {rexi_app,[]}}
]}.
